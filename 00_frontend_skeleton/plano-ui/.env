# PlanO UI — Environment Configuration
# Raster engine on hadrien-skoed-mt (GPU server) via SSH tunnel
VITE_RASTER_URL=/api/raster
# React Planner engine (same-origin iframe)
VITE_ENGINE_URL=http://localhost:5173
